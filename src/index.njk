---
layout: null
title: Louisenfest Archiv
---

<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8">
  <title>{{ title }}</title>
  <style>
    .gallery { display:grid; grid-template-columns: repeat(auto-fit,minmax(250px,1fr)); gap:1rem; }
    .gallery img, .gallery video { width:100%; border-radius:8px;}
  </style>
</head>
<body>
  <h1>🎉 Louisenfest Archiv</h1>
  <div><a href="/admin/">Medien hochladen (Admin)</a></div>
<div class="gallery">
  {% for file in collections.mediaFiles %}
    {% set ext = file.name.split('.').pop().toLowerCase() %}
    {% if ext in ["jpg","jpeg","png","webp","gif"] %}
      <img src="/media/{{ file.name }}" alt="{{ file.name }}">
    {% elif ext in ["mp4","webm"] %}
      <video controls src="/media/{{ file.name }}"></video>
    {% endif %}
  {% endfor %}
</div>
</body>
</html>
